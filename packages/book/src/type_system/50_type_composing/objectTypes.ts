interface User {
  id: string;
}

interface Student {
  id: string;
}

interface Employee {
  id: string;
  name: string;
}

interface Member {
  memberId: number;
}

export type { User, Student, Member, Employee };
